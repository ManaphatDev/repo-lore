// Test stub for the `server-only` package, which throws when bundled outside a
// React Server Component. Aliased in vitest.config.mts so server modules can be
// imported in the Node test environment.
export {};
